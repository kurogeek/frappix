#!/bin/sh

# Enter the frappix development shell for the current system.
# Entering the shell generates the repo dotfiles via its startup hooks
# (formerly `use envreload //local/shells/default //local/config`).
use flake
