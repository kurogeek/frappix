name: CI

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: nixbuild/nix-quick-install-action@v29
      - uses: DeterminateSystems/magic-nix-cache-action@main
      # Evaluate the whole flake schema (fast, catches wiring regressions).
      - name: Evaluate flake
        run: nix flake show --all-systems
      # Build the tools of the frappix package set used by the weekly job.
      - name: Build nvchecker-nix
        run: nix build .#nvchecker-nix
      # Make sure the dev shells still evaluate/activate.
      - name: Evaluate dev shells
        run: |
          nix eval --raw .#devShells.x86_64-linux.default.name
          nix eval --raw .#devShells.x86_64-linux.book.name
